# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project

This is a new Vue.js project directory. The project has not been scaffolded yet.

To initialize a Vue project here, run:

```bash
npm create vue@latest .
```

Or with Vite directly:

```bash
npm create vite@latest . -- --template vue
```

Once scaffolded, update this file with the actual build, dev, and test commands from `package.json`.
