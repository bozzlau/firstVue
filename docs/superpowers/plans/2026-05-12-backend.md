# Blog Backend Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build a FastAPI backend with JWT auth, SQLite database, and REST API for a personal blog.

**Architecture:** FastAPI app with SQLAlchemy ORM for data, Alembic for migrations, Pydantic schemas for API validation, and JWT stored in httpOnly cookies for auth. Public routes are open; admin routes require a valid token.

**Tech Stack:** Python 3.11+, FastAPI, SQLAlchemy 2.x, Alembic, Pydantic v2, python-jose (JWT), passlib (password hashing), pytest, httpx (test client)

---

## File Map

```
backend/
├── app/
│   ├── main.py              # FastAPI app, CORS, router registration
│   ├── config.py            # Settings from env vars (pydantic-settings)
│   ├── database.py          # SQLAlchemy engine + session factory
│   ├── dependencies.py      # get_db(), get_current_user()
│   ├── models/
│   │   ├── __init__.py
│   │   ├── user.py
│   │   ├── category.py
│   │   ├── tag.py
│   │   ├── post.py          # includes post_tags association table
│   │   ├── comment.py
│   │   └── post_log.py
│   ├── schemas/
│   │   ├── __init__.py
│   │   ├── user.py
│   │   ├── category.py
│   │   ├── tag.py
│   │   ├── post.py
│   │   └── comment.py
│   ├── routers/
│   │   ├── public/
│   │   │   ├── __init__.py
│   │   │   ├── posts.py
│   │   │   ├── categories.py
│   │   │   ├── tags.py
│   │   │   └── comments.py
│   │   └── admin/
│   │       ├── __init__.py
│   │       ├── auth.py
│   │       ├── posts.py
│   │       ├── categories.py
│   │       ├── tags.py
│   │       └── comments.py
│   └── services/
│       ├── auth.py          # hash_password, verify_password, create_token, decode_token
│       └── post.py          # create_post, delete_post (soft), restore_post, log_action
├── alembic/
│   ├── env.py
│   └── versions/
├── tests/
│   ├── conftest.py          # test DB, test client, fixtures
│   ├── test_public_posts.py
│   ├── test_public_categories.py
│   ├── test_auth.py
│   └── test_admin_posts.py
├── alembic.ini
├── requirements.txt
└── .env

---

## Task 1: 初始化项目结构

**Files:**
- Create: `backend/requirements.txt`
- Create: `backend/.env`
- Create: `backend/app/__init__.py`
- Create: `backend/app/config.py`
- Create: `backend/app/main.py`

- [ ] **Step 1: 创建目录结构**

```bash
cd /Users/lyb/claudeTest/firstVue
mkdir -p backend/app/models backend/app/schemas
mkdir -p backend/app/routers/public backend/app/routers/admin
mkdir -p backend/app/services backend/tests
touch backend/app/__init__.py
touch backend/app/models/__init__.py backend/app/schemas/__init__.py
touch backend/app/routers/__init__.py
touch backend/app/routers/public/__init__.py
touch backend/app/routers/admin/__init__.py
touch backend/app/services/__init__.py
```

- [ ] **Step 2: 创建 requirements.txt**

```
fastapi==0.111.0
uvicorn[standard]==0.29.0
sqlalchemy==2.0.30
alembic==1.13.1
pydantic-settings==2.2.1
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.9
pytest==8.2.0
httpx==0.27.0
```

安装依赖：
```bash
cd backend
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

- [ ] **Step 3: 创建 .env**

```
SECRET_KEY=your-super-secret-key-change-this-in-production-min-32-chars
DATABASE_URL=sqlite:///./blog.db
ADMIN_USERNAME=admin
ADMIN_PASSWORD=changeme123
ACCESS_TOKEN_EXPIRE_MINUTES=60
```

- [ ] **Step 4: 创建 app/config.py**

```python
from pydantic_settings import BaseSettings

class Settings(BaseSettings):
    secret_key: str
    database_url: str = "sqlite:///./blog.db"
    admin_username: str = "admin"
    admin_password: str = "changeme123"
    access_token_expire_minutes: int = 60

    class Config:
        env_file = ".env"

settings = Settings()
```

- [ ] **Step 5: 创建 app/main.py**

```python
from fastapi import FastAPI
from fastapi.middleware.cors import CORSMiddleware

app = FastAPI(title="Personal Blog API", version="1.0.0")

app.add_middleware(
    CORSMiddleware,
    allow_origins=["http://localhost:5173"],
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)

@app.get("/health")
def health():
    return {"status": "ok"}
```

- [ ] **Step 6: 启动并验证**

```bash
cd backend
uvicorn app.main:app --reload
```

打开浏览器访问 `http://localhost:8000/health`，应看到 `{"status":"ok"}`。
访问 `http://localhost:8000/docs` 应看到 Swagger UI。

- [ ] **Step 7: Commit**

```bash
git init
echo "venv/" > .gitignore
echo "__pycache__/" >> .gitignore
echo "*.db" >> .gitignore
echo ".env" >> .gitignore
git add .
git commit -m "feat: initialize FastAPI project structure"
```


---

## Task 2: 数据库连接和模型定义

**Files:**
- Create: `backend/app/database.py`
- Create: `backend/app/models/user.py`
- Create: `backend/app/models/category.py`
- Create: `backend/app/models/tag.py`
- Create: `backend/app/models/post.py`
- Create: `backend/app/models/comment.py`
- Create: `backend/app/models/post_log.py`
- Modify: `backend/app/models/__init__.py`

- [ ] **Step 1: 创建 app/database.py**

```python
from sqlalchemy import create_engine
from sqlalchemy.orm import DeclarativeBase, sessionmaker
from app.config import settings

engine = create_engine(
    settings.database_url,
    connect_args={"check_same_thread": False}  # SQLite 专用
)
SessionLocal = sessionmaker(autocommit=False, autoflush=False, bind=engine)

class Base(DeclarativeBase):
    pass
```

- [ ] **Step 2: 创建 app/models/user.py**

```python
from datetime import datetime
from sqlalchemy import String, DateTime
from sqlalchemy.orm import Mapped, mapped_column
from app.database import Base

class User(Base):
    __tablename__ = "users"

    id: Mapped[int] = mapped_column(primary_key=True)
    username: Mapped[str] = mapped_column(String(50), unique=True, index=True)
    email: Mapped[str] = mapped_column(String(100), unique=True)
    hashed_password: Mapped[str] = mapped_column(String(200))
    created_at: Mapped[datetime] = mapped_column(DateTime, default=datetime.utcnow)
```

- [ ] **Step 3: 创建 app/models/category.py**

```python
from sqlalchemy import String
from sqlalchemy.orm import Mapped, mapped_column, relationship
from app.database import Base

class Category(Base):
    __tablename__ = "categories"

    id: Mapped[int] = mapped_column(primary_key=True)
    name: Mapped[str] = mapped_column(String(50), unique=True)
    slug: Mapped[str] = mapped_column(String(50), unique=True, index=True)
    description: Mapped[str | None] = mapped_column(String(200))

    posts: Mapped[list["Post"]] = relationship("Post", back_populates="category")
```

- [ ] **Step 4: 创建 app/models/tag.py**

```python
from sqlalchemy import String
from sqlalchemy.orm import Mapped, mapped_column, relationship
from app.database import Base

class Tag(Base):
    __tablename__ = "tags"

    id: Mapped[int] = mapped_column(primary_key=True)
    name: Mapped[str] = mapped_column(String(50), unique=True)
    slug: Mapped[str] = mapped_column(String(50), unique=True, index=True)

    posts: Mapped[list["Post"]] = relationship("Post", secondary="post_tags", back_populates="tags")
```

- [ ] **Step 5: 创建 app/models/post.py**

```python
from datetime import datetime
from sqlalchemy import String, Text, Integer, ForeignKey, Table, Column, DateTime
from sqlalchemy.orm import Mapped, mapped_column, relationship
from app.database import Base

post_tags = Table(
    "post_tags",
    Base.metadata,
    Column("post_id", Integer, ForeignKey("posts.id"), primary_key=True),
    Column("tag_id", Integer, ForeignKey("tags.id"), primary_key=True),
)

class Post(Base):
    __tablename__ = "posts"

    id: Mapped[int] = mapped_column(primary_key=True)
    title: Mapped[str] = mapped_column(String(200))
    slug: Mapped[str] = mapped_column(String(200), unique=True, index=True)
    content: Mapped[str] = mapped_column(Text)
    summary: Mapped[str | None] = mapped_column(String(500))
    cover_image: Mapped[str | None] = mapped_column(String(500))
    status: Mapped[str] = mapped_column(String(20), default="draft")  # draft | published
    views: Mapped[int] = mapped_column(Integer, default=0)
    author_id: Mapped[int] = mapped_column(ForeignKey("users.id"))
    category_id: Mapped[int | None] = mapped_column(ForeignKey("categories.id"))
    deleted_at: Mapped[datetime | None] = mapped_column(DateTime, nullable=True)
    created_at: Mapped[datetime] = mapped_column(DateTime, default=datetime.utcnow)
    updated_at: Mapped[datetime] = mapped_column(DateTime, default=datetime.utcnow, onupdate=datetime.utcnow)

    author: Mapped["User"] = relationship("User")
    category: Mapped["Category | None"] = relationship("Category", back_populates="posts")
    tags: Mapped[list["Tag"]] = relationship("Tag", secondary=post_tags, back_populates="posts")
    comments: Mapped[list["Comment"]] = relationship("Comment", back_populates="post")
    logs: Mapped[list["PostLog"]] = relationship("PostLog", back_populates="post")
```

- [ ] **Step 6: 创建 app/models/comment.py**

```python
from datetime import datetime
from sqlalchemy import String, Text, Boolean, ForeignKey, DateTime
from sqlalchemy.orm import Mapped, mapped_column, relationship
from app.database import Base

class Comment(Base):
    __tablename__ = "comments"

    id: Mapped[int] = mapped_column(primary_key=True)
    post_id: Mapped[int] = mapped_column(ForeignKey("posts.id"))
    author_name: Mapped[str] = mapped_column(String(100))
    author_email: Mapped[str] = mapped_column(String(100))
    content: Mapped[str] = mapped_column(Text)
    is_approved: Mapped[bool] = mapped_column(Boolean, default=False)
    created_at: Mapped[datetime] = mapped_column(DateTime, default=datetime.utcnow)

    post: Mapped["Post"] = relationship("Post", back_populates="comments")
```

- [ ] **Step 7: 创建 app/models/post_log.py**

```python
from datetime import datetime
from sqlalchemy import String, Text, ForeignKey, DateTime
from sqlalchemy.orm import Mapped, mapped_column, relationship
from app.database import Base

class PostLog(Base):
    __tablename__ = "post_logs"

    id: Mapped[int] = mapped_column(primary_key=True)
    post_id: Mapped[int] = mapped_column(ForeignKey("posts.id"))
    action: Mapped[str] = mapped_column(String(20))  # published | deleted | restored
    operated_at: Mapped[datetime] = mapped_column(DateTime, default=datetime.utcnow)
    note: Mapped[str | None] = mapped_column(Text, nullable=True)

    post: Mapped["Post"] = relationship("Post", back_populates="logs")
```

- [ ] **Step 8: 更新 app/models/__init__.py**

```python
from app.models.user import User
from app.models.category import Category
from app.models.tag import Tag
from app.models.post import Post, post_tags
from app.models.comment import Comment
from app.models.post_log import PostLog
```

- [ ] **Step 9: Commit**

```bash
git add app/database.py app/models/
git commit -m "feat: define SQLAlchemy database models"
```


---

## Task 3: Alembic 数据库迁移

**Files:**
- Create: `backend/alembic.ini`
- Create: `backend/alembic/env.py`
- Create: `backend/app/dependencies.py`

- [ ] **Step 1: 初始化 Alembic**

```bash
cd backend
alembic init alembic
```

- [ ] **Step 2: 修改 alembic/env.py**

找到 `env.py` 里的 `target_metadata = None` 这一行，替换整个文件关键部分：

```python
import sys
import os
sys.path.insert(0, os.path.dirname(os.path.dirname(__file__)))

from app.database import Base
from app.models import User, Category, Tag, Post, Comment, PostLog  # noqa: F401
from app.config import settings

# 找到这行并替换：
# target_metadata = None
target_metadata = Base.metadata

# 找到 run_migrations_online() 里的 connectable，替换为：
connectable = create_engine(settings.database_url, connect_args={"check_same_thread": False})
```

- [ ] **Step 3: 生成第一个迁移文件**

```bash
alembic revision --autogenerate -m "initial tables"
```

预期输出：`Generating .../alembic/versions/xxxx_initial_tables.py`

- [ ] **Step 4: 执行迁移（建表）**

```bash
alembic upgrade head
```

预期输出：`Running upgrade  -> xxxx, initial tables`

验证数据库已创建：
```bash
python -c "from app.database import engine; from sqlalchemy import inspect; print(inspect(engine).get_table_names())"
```

预期输出：`['categories', 'comments', 'post_logs', 'post_tags', 'posts', 'tags', 'users']`

- [ ] **Step 5: 创建 app/dependencies.py**

```python
from typing import Generator
from fastapi import Depends, HTTPException, Cookie
from sqlalchemy.orm import Session
from app.database import SessionLocal
from app.services.auth import decode_token
from app.models.user import User

def get_db() -> Generator:
    db = SessionLocal()
    try:
        yield db
    finally:
        db.close()

def get_current_user(
    access_token: str | None = Cookie(default=None),
    db: Session = Depends(get_db),
) -> User:
    if not access_token:
        raise HTTPException(status_code=401, detail="Not authenticated")
    username = decode_token(access_token)
    user = db.query(User).filter(User.username == username).first()
    if not user:
        raise HTTPException(status_code=401, detail="User not found")
    return user
```

- [ ] **Step 6: Commit**

```bash
git add alembic/ alembic.ini app/dependencies.py
git commit -m "feat: add Alembic migrations and DB session dependency"
```


---

## Task 4: 认证服务（JWT）

**Files:**
- Create: `backend/app/services/auth.py`

- [ ] **Step 1: 创建 app/services/auth.py**

```python
from datetime import datetime, timedelta
from jose import JWTError, jwt
from passlib.context import CryptContext
from app.config import settings

pwd_context = CryptContext(schemes=["bcrypt"], deprecated="auto")

def hash_password(password: str) -> str:
    return pwd_context.hash(password)

def verify_password(plain: str, hashed: str) -> bool:
    return pwd_context.verify(plain, hashed)

def create_access_token(username: str) -> str:
    expire = datetime.utcnow() + timedelta(minutes=settings.access_token_expire_minutes)
    payload = {"sub": username, "exp": expire}
    return jwt.encode(payload, settings.secret_key, algorithm="HS256")

def decode_token(token: str) -> str:
    try:
        payload = jwt.decode(token, settings.secret_key, algorithms=["HS256"])
        username: str = payload.get("sub")
        if username is None:
            raise ValueError("Invalid token")
        return username
    except JWTError:
        raise ValueError("Invalid or expired token")
```

- [ ] **Step 2: 写测试**

新建 `tests/test_auth.py`：

```python
from app.services.auth import hash_password, verify_password, create_access_token, decode_token

def test_password_hash_and_verify():
    hashed = hash_password("secret123")
    assert verify_password("secret123", hashed)
    assert not verify_password("wrong", hashed)

def test_token_roundtrip():
    token = create_access_token("admin")
    assert decode_token(token) == "admin"

def test_invalid_token_raises():
    import pytest
    with pytest.raises(ValueError):
        decode_token("not-a-real-token")
```

- [ ] **Step 3: 运行测试**

```bash
cd backend
pytest tests/test_auth.py -v
```

预期：3 tests PASSED

- [ ] **Step 4: Commit**

```bash
git add app/services/auth.py tests/test_auth.py
git commit -m "feat: add JWT auth service with password hashing"
```

---

## Task 5: Pydantic Schemas

**Files:**
- Create: `backend/app/schemas/user.py`
- Create: `backend/app/schemas/category.py`
- Create: `backend/app/schemas/tag.py`
- Create: `backend/app/schemas/post.py`
- Create: `backend/app/schemas/comment.py`
- Modify: `backend/app/schemas/__init__.py`

- [ ] **Step 1: 创建 app/schemas/user.py**

```python
from pydantic import BaseModel

class UserLogin(BaseModel):
    username: str
    password: str

class UserResponse(BaseModel):
    id: int
    username: str
    email: str

    model_config = {"from_attributes": True}
```

- [ ] **Step 2: 创建 app/schemas/category.py**

```python
from pydantic import BaseModel

class CategoryBase(BaseModel):
    name: str
    slug: str
    description: str | None = None

class CategoryCreate(CategoryBase):
    pass

class CategoryResponse(CategoryBase):
    id: int
    model_config = {"from_attributes": True}
```

- [ ] **Step 3: 创建 app/schemas/tag.py**

```python
from pydantic import BaseModel

class TagBase(BaseModel):
    name: str
    slug: str

class TagCreate(TagBase):
    pass

class TagResponse(TagBase):
    id: int
    model_config = {"from_attributes": True}
```

- [ ] **Step 4: 创建 app/schemas/post.py**

```python
from datetime import datetime
from pydantic import BaseModel
from app.schemas.category import CategoryResponse
from app.schemas.tag import TagResponse

class PostCreate(BaseModel):
    title: str
    slug: str
    content: str
    summary: str | None = None
    cover_image: str | None = None
    status: str = "draft"
    category_id: int | None = None
    tag_ids: list[int] = []

class PostUpdate(BaseModel):
    title: str | None = None
    slug: str | None = None
    content: str | None = None
    summary: str | None = None
    cover_image: str | None = None
    status: str | None = None
    category_id: int | None = None
    tag_ids: list[int] | None = None

class PostResponse(BaseModel):
    id: int
    title: str
    slug: str
    summary: str | None
    cover_image: str | None
    status: str
    views: int
    category: CategoryResponse | None
    tags: list[TagResponse]
    created_at: datetime
    updated_at: datetime
    model_config = {"from_attributes": True}

class PostDetail(PostResponse):
    content: str
```

- [ ] **Step 5: 创建 app/schemas/comment.py**

```python
from datetime import datetime
from pydantic import BaseModel, EmailStr

class CommentCreate(BaseModel):
    author_name: str
    author_email: EmailStr
    content: str

class CommentResponse(BaseModel):
    id: int
    author_name: str
    content: str
    created_at: datetime
    model_config = {"from_attributes": True}

class CommentAdminResponse(CommentResponse):
    author_email: str
    is_approved: bool
    post_id: int
```

- [ ] **Step 6: 更新 app/schemas/__init__.py**

```python
from app.schemas.user import UserLogin, UserResponse
from app.schemas.category import CategoryCreate, CategoryResponse
from app.schemas.tag import TagCreate, TagResponse
from app.schemas.post import PostCreate, PostUpdate, PostResponse, PostDetail
from app.schemas.comment import CommentCreate, CommentResponse, CommentAdminResponse
```

- [ ] **Step 7: Commit**

```bash
git add app/schemas/
git commit -m "feat: add Pydantic schemas for all resources"
```


---

## Task 6: Post 业务逻辑服务

**Files:**
- Create: `backend/app/services/post.py`

- [ ] **Step 1: 创建 app/services/post.py**

```python
from datetime import datetime
from sqlalchemy.orm import Session
from app.models.post import Post
from app.models.post_log import PostLog
from app.models.tag import Tag
from app.schemas.post import PostCreate, PostUpdate

def _log(db: Session, post_id: int, action: str, note: str | None = None):
    db.add(PostLog(post_id=post_id, action=action, note=note))

def create_post(db: Session, data: PostCreate, author_id: int) -> Post:
    tags = db.query(Tag).filter(Tag.id.in_(data.tag_ids)).all()
    post = Post(
        title=data.title,
        slug=data.slug,
        content=data.content,
        summary=data.summary,
        cover_image=data.cover_image,
        status=data.status,
        category_id=data.category_id,
        author_id=author_id,
        tags=tags,
    )
    db.add(post)
    db.flush()
    _log(db, post.id, data.status)
    db.commit()
    db.refresh(post)
    return post

def update_post(db: Session, post: Post, data: PostUpdate) -> Post:
    for field, value in data.model_dump(exclude_unset=True).items():
        if field == "tag_ids":
            post.tags = db.query(Tag).filter(Tag.id.in_(value)).all()
        else:
            setattr(post, field, value)
    if data.status:
        _log(db, post.id, data.status)
    db.commit()
    db.refresh(post)
    return post

def soft_delete(db: Session, post: Post, note: str | None = None) -> Post:
    post.deleted_at = datetime.utcnow()
    _log(db, post.id, "deleted", note)
    db.commit()
    db.refresh(post)
    return post

def restore_post(db: Session, post: Post, note: str | None = None) -> Post:
    post.deleted_at = None
    _log(db, post.id, "restored", note)
    db.commit()
    db.refresh(post)
    return post
```

- [ ] **Step 2: Commit**

```bash
git add app/services/post.py
git commit -m "feat: add post service with soft delete and audit logging"
```

---

## Task 7: 公开 API 路由

**Files:**
- Create: `backend/app/routers/public/posts.py`
- Create: `backend/app/routers/public/categories.py`
- Create: `backend/app/routers/public/tags.py`
- Create: `backend/app/routers/public/comments.py`
- Modify: `backend/app/main.py`

- [ ] **Step 1: 创建 app/routers/public/posts.py**

```python
from fastapi import APIRouter, Depends, HTTPException, Query
from sqlalchemy.orm import Session
from app.dependencies import get_db
from app.models.post import Post
from app.schemas.post import PostResponse, PostDetail

router = APIRouter(prefix="/api/posts", tags=["public-posts"])

@router.get("", response_model=list[PostResponse])
def list_posts(
    page: int = Query(1, ge=1),
    size: int = Query(10, ge=1, le=50),
    category: str | None = None,
    tag: str | None = None,
    db: Session = Depends(get_db),
):
    q = db.query(Post).filter(Post.deleted_at.is_(None), Post.status == "published")
    if category:
        q = q.join(Post.category).filter_by(slug=category)
    if tag:
        q = q.join(Post.tags).filter_by(slug=tag)
    return q.order_by(Post.created_at.desc()).offset((page - 1) * size).limit(size).all()

@router.get("/{slug}", response_model=PostDetail)
def get_post(slug: str, db: Session = Depends(get_db)):
    post = db.query(Post).filter(
        Post.slug == slug,
        Post.deleted_at.is_(None),
        Post.status == "published",
    ).first()
    if not post:
        raise HTTPException(status_code=404, detail="Post not found")
    post.views += 1
    db.commit()
    db.refresh(post)
    return post
```

- [ ] **Step 2: 创建 app/routers/public/categories.py**

```python
from fastapi import APIRouter, Depends
from sqlalchemy.orm import Session
from app.dependencies import get_db
from app.models.category import Category
from app.schemas.category import CategoryResponse

router = APIRouter(prefix="/api/categories", tags=["public-categories"])

@router.get("", response_model=list[CategoryResponse])
def list_categories(db: Session = Depends(get_db)):
    return db.query(Category).all()
```

- [ ] **Step 3: 创建 app/routers/public/tags.py**

```python
from fastapi import APIRouter, Depends
from sqlalchemy.orm import Session
from app.dependencies import get_db
from app.models.tag import Tag
from app.schemas.tag import TagResponse

router = APIRouter(prefix="/api/tags", tags=["public-tags"])

@router.get("", response_model=list[TagResponse])
def list_tags(db: Session = Depends(get_db)):
    return db.query(Tag).all()
```

- [ ] **Step 4: 创建 app/routers/public/comments.py**

```python
from fastapi import APIRouter, Depends, HTTPException
from sqlalchemy.orm import Session
from app.dependencies import get_db
from app.models.post import Post
from app.models.comment import Comment
from app.schemas.comment import CommentCreate, CommentResponse

router = APIRouter(prefix="/api/posts", tags=["public-comments"])

@router.get("/{slug}/comments", response_model=list[CommentResponse])
def list_comments(slug: str, db: Session = Depends(get_db)):
    post = db.query(Post).filter(Post.slug == slug, Post.deleted_at.is_(None)).first()
    if not post:
        raise HTTPException(status_code=404, detail="Post not found")
    return db.query(Comment).filter(
        Comment.post_id == post.id, Comment.is_approved == True
    ).order_by(Comment.created_at).all()

@router.post("/{slug}/comments", response_model=CommentResponse, status_code=201)
def create_comment(slug: str, data: CommentCreate, db: Session = Depends(get_db)):
    post = db.query(Post).filter(Post.slug == slug, Post.deleted_at.is_(None)).first()
    if not post:
        raise HTTPException(status_code=404, detail="Post not found")
    comment = Comment(post_id=post.id, **data.model_dump())
    db.add(comment)
    db.commit()
    db.refresh(comment)
    return comment
```

- [ ] **Step 5: 注册路由到 app/main.py**

```python
from fastapi import FastAPI
from fastapi.middleware.cors import CORSMiddleware
from app.routers.public import posts, categories, tags, comments

app = FastAPI(title="Personal Blog API", version="1.0.0")

app.add_middleware(
    CORSMiddleware,
    allow_origins=["http://localhost:5173"],
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)

app.include_router(posts.router)
app.include_router(categories.router)
app.include_router(tags.router)
app.include_router(comments.router)

@app.get("/health")
def health():
    return {"status": "ok"}
```

- [ ] **Step 6: Commit**

```bash
git add app/routers/public/ app/main.py
git commit -m "feat: add public API routes for posts, categories, tags, comments"
```


---

## Task 8: 管理员登录 API

**Files:**
- Create: `backend/app/routers/admin/auth.py`
- Modify: `backend/app/main.py`

- [ ] **Step 1: 创建初始管理员用户的脚本**

新建 `backend/create_admin.py`：

```python
from app.database import SessionLocal
from app.models.user import User
from app.services.auth import hash_password
from app.config import settings

db = SessionLocal()
existing = db.query(User).filter(User.username == settings.admin_username).first()
if not existing:
    user = User(
        username=settings.admin_username,
        email="admin@blog.local",
        hashed_password=hash_password(settings.admin_password),
    )
    db.add(user)
    db.commit()
    print(f"Admin user '{settings.admin_username}' created.")
else:
    print("Admin user already exists.")
db.close()
```

运行：
```bash
cd backend
python create_admin.py
```

预期输出：`Admin user 'admin' created.`

- [ ] **Step 2: 创建 app/routers/admin/auth.py**

```python
from fastapi import APIRouter, Depends, HTTPException, Response
from sqlalchemy.orm import Session
from app.dependencies import get_db
from app.models.user import User
from app.schemas.user import UserLogin
from app.services.auth import verify_password, create_access_token
from app.config import settings

router = APIRouter(prefix="/api/auth", tags=["auth"])

@router.post("/login")
def login(data: UserLogin, response: Response, db: Session = Depends(get_db)):
    user = db.query(User).filter(User.username == data.username).first()
    if not user or not verify_password(data.password, user.hashed_password):
        raise HTTPException(status_code=401, detail="Invalid credentials")
    token = create_access_token(user.username)
    response.set_cookie(
        key="access_token",
        value=token,
        httponly=True,
        max_age=settings.access_token_expire_minutes * 60,
        samesite="lax",
    )
    return {"message": "Login successful"}

@router.post("/logout")
def logout(response: Response):
    response.delete_cookie("access_token")
    return {"message": "Logged out"}
```

- [ ] **Step 3: 注册路由到 app/main.py**

在 `main.py` 的 import 区域添加：
```python
from app.routers.admin import auth as admin_auth
```

在 `include_router` 区域添加：
```python
app.include_router(admin_auth.router)
```

- [ ] **Step 4: 写测试**

新建 `tests/conftest.py`：

```python
import pytest
from fastapi.testclient import TestClient
from sqlalchemy import create_engine
from sqlalchemy.orm import sessionmaker
from app.main import app
from app.database import Base
from app.dependencies import get_db
from app.models.user import User
from app.services.auth import hash_password

TEST_DATABASE_URL = "sqlite:///./test.db"
engine = create_engine(TEST_DATABASE_URL, connect_args={"check_same_thread": False})
TestingSessionLocal = sessionmaker(autocommit=False, autoflush=False, bind=engine)

@pytest.fixture(autouse=True)
def setup_db():
    Base.metadata.create_all(bind=engine)
    yield
    Base.metadata.drop_all(bind=engine)

@pytest.fixture
def db():
    session = TestingSessionLocal()
    try:
        yield session
    finally:
        session.close()

@pytest.fixture
def client(db):
    def override_get_db():
        try:
            yield db
        finally:
            pass
    app.dependency_overrides[get_db] = override_get_db
    with TestClient(app) as c:
        yield c
    app.dependency_overrides.clear()

@pytest.fixture
def admin_user(db):
    user = User(
        username="admin",
        email="admin@test.com",
        hashed_password=hash_password("testpass123"),
    )
    db.add(user)
    db.commit()
    db.refresh(user)
    return user

@pytest.fixture
def auth_client(client, admin_user):
    client.post("/api/auth/login", json={"username": "admin", "password": "testpass123"})
    return client
```

新建 `tests/test_auth.py`（追加登录测试）：

```python
def test_login_success(client, admin_user):
    res = client.post("/api/auth/login", json={"username": "admin", "password": "testpass123"})
    assert res.status_code == 200
    assert "access_token" in res.cookies

def test_login_wrong_password(client, admin_user):
    res = client.post("/api/auth/login", json={"username": "admin", "password": "wrong"})
    assert res.status_code == 401

def test_logout(auth_client):
    res = auth_client.post("/api/auth/logout")
    assert res.status_code == 200
```

- [ ] **Step 5: 运行测试**

```bash
pytest tests/test_auth.py -v
```

预期：所有测试 PASSED

- [ ] **Step 6: Commit**

```bash
git add app/routers/admin/auth.py app/main.py tests/conftest.py tests/test_auth.py create_admin.py
git commit -m "feat: add admin login/logout with JWT cookie"
```


---

## Task 9: 管理员文章 API

**Files:**
- Create: `backend/app/routers/admin/posts.py`
- Modify: `backend/app/main.py`

- [ ] **Step 1: 创建 app/routers/admin/posts.py**

```python
from fastapi import APIRouter, Depends, HTTPException
from sqlalchemy.orm import Session
from app.dependencies import get_db, get_current_user
from app.models.post import Post
from app.models.post_log import PostLog
from app.models.user import User
from app.schemas.post import PostCreate, PostUpdate, PostDetail, PostResponse
from app.services.post import create_post, update_post, soft_delete, restore_post

router = APIRouter(prefix="/api/admin/posts", tags=["admin-posts"])

@router.get("", response_model=list[PostResponse])
def list_all_posts(
    db: Session = Depends(get_db),
    _: User = Depends(get_current_user),
):
    return db.query(Post).order_by(Post.created_at.desc()).all()

@router.post("", response_model=PostDetail, status_code=201)
def create(
    data: PostCreate,
    db: Session = Depends(get_db),
    current_user: User = Depends(get_current_user),
):
    return create_post(db, data, current_user.id)

@router.get("/{post_id}", response_model=PostDetail)
def get_post(
    post_id: int,
    db: Session = Depends(get_db),
    _: User = Depends(get_current_user),
):
    post = db.query(Post).filter(Post.id == post_id).first()
    if not post:
        raise HTTPException(status_code=404, detail="Post not found")
    return post

@router.put("/{post_id}", response_model=PostDetail)
def update(
    post_id: int,
    data: PostUpdate,
    db: Session = Depends(get_db),
    _: User = Depends(get_current_user),
):
    post = db.query(Post).filter(Post.id == post_id).first()
    if not post:
        raise HTTPException(status_code=404, detail="Post not found")
    return update_post(db, post, data)

@router.delete("/{post_id}")
def delete(
    post_id: int,
    db: Session = Depends(get_db),
    _: User = Depends(get_current_user),
):
    post = db.query(Post).filter(Post.id == post_id, Post.deleted_at.is_(None)).first()
    if not post:
        raise HTTPException(status_code=404, detail="Post not found")
    soft_delete(db, post)
    return {"message": "Post deleted"}

@router.post("/{post_id}/restore")
def restore(
    post_id: int,
    db: Session = Depends(get_db),
    _: User = Depends(get_current_user),
):
    post = db.query(Post).filter(Post.id == post_id, Post.deleted_at.isnot(None)).first()
    if not post:
        raise HTTPException(status_code=404, detail="Deleted post not found")
    restore_post(db, post)
    return {"message": "Post restored"}

@router.get("/{post_id}/logs")
def get_logs(
    post_id: int,
    db: Session = Depends(get_db),
    _: User = Depends(get_current_user),
):
    return db.query(PostLog).filter(PostLog.post_id == post_id).order_by(PostLog.operated_at).all()
```

- [ ] **Step 2: 注册路由到 app/main.py**

```python
from app.routers.admin import posts as admin_posts
# ...
app.include_router(admin_posts.router)
```

- [ ] **Step 3: 写测试**

新建 `tests/test_admin_posts.py`：

```python
def test_create_post_requires_auth(client):
    res = client.post("/api/admin/posts", json={
        "title": "Test", "slug": "test", "content": "Hello"
    })
    assert res.status_code == 401

def test_create_post(auth_client):
    res = auth_client.post("/api/admin/posts", json={
        "title": "My First Post",
        "slug": "my-first-post",
        "content": "# Hello World",
        "status": "published",
    })
    assert res.status_code == 201
    assert res.json()["slug"] == "my-first-post"

def test_soft_delete_and_restore(auth_client):
    # 创建文章
    res = auth_client.post("/api/admin/posts", json={
        "title": "To Delete", "slug": "to-delete", "content": "bye"
    })
    post_id = res.json()["id"]

    # 软删除
    res = auth_client.delete(f"/api/admin/posts/{post_id}")
    assert res.status_code == 200

    # 公开接口看不到已删除文章
    res = auth_client.get("/api/posts/to-delete")
    assert res.status_code == 404

    # 恢复
    res = auth_client.post(f"/api/admin/posts/{post_id}/restore")
    assert res.status_code == 200

def test_post_logs_recorded(auth_client):
    res = auth_client.post("/api/admin/posts", json={
        "title": "Log Test", "slug": "log-test", "content": "x", "status": "published"
    })
    post_id = res.json()["id"]
    auth_client.delete(f"/api/admin/posts/{post_id}")
    auth_client.post(f"/api/admin/posts/{post_id}/restore")

    logs = auth_client.get(f"/api/admin/posts/{post_id}/logs").json()
    actions = [l["action"] for l in logs]
    assert actions == ["published", "deleted", "restored"]
```

- [ ] **Step 4: 运行测试**

```bash
pytest tests/test_admin_posts.py -v
```

预期：4 tests PASSED

- [ ] **Step 5: Commit**

```bash
git add app/routers/admin/posts.py app/main.py tests/test_admin_posts.py
git commit -m "feat: add admin post CRUD with soft delete and restore"
```


---

## Task 10: 管理员分类和标签 API

**Files:**
- Create: `backend/app/routers/admin/categories.py`
- Create: `backend/app/routers/admin/tags.py`
- Modify: `backend/app/main.py`

- [ ] **Step 1: 创建 app/routers/admin/categories.py**

```python
from fastapi import APIRouter, Depends, HTTPException
from sqlalchemy.orm import Session
from app.dependencies import get_db, get_current_user
from app.models.category import Category
from app.models.user import User
from app.schemas.category import CategoryCreate, CategoryResponse

router = APIRouter(prefix="/api/admin/categories", tags=["admin-categories"])

@router.post("", response_model=CategoryResponse, status_code=201)
def create(data: CategoryCreate, db: Session = Depends(get_db), _: User = Depends(get_current_user)):
    if db.query(Category).filter(Category.slug == data.slug).first():
        raise HTTPException(status_code=400, detail="Slug already exists")
    cat = Category(**data.model_dump())
    db.add(cat)
    db.commit()
    db.refresh(cat)
    return cat

@router.put("/{cat_id}", response_model=CategoryResponse)
def update(cat_id: int, data: CategoryCreate, db: Session = Depends(get_db), _: User = Depends(get_current_user)):
    cat = db.query(Category).filter(Category.id == cat_id).first()
    if not cat:
        raise HTTPException(status_code=404, detail="Category not found")
    for field, value in data.model_dump().items():
        setattr(cat, field, value)
    db.commit()
    db.refresh(cat)
    return cat

@router.delete("/{cat_id}")
def delete(cat_id: int, db: Session = Depends(get_db), _: User = Depends(get_current_user)):
    cat = db.query(Category).filter(Category.id == cat_id).first()
    if not cat:
        raise HTTPException(status_code=404, detail="Category not found")
    db.delete(cat)
    db.commit()
    return {"message": "Category deleted"}
```

- [ ] **Step 2: 创建 app/routers/admin/tags.py**

```python
from fastapi import APIRouter, Depends, HTTPException
from sqlalchemy.orm import Session
from app.dependencies import get_db, get_current_user
from app.models.tag import Tag
from app.models.user import User
from app.schemas.tag import TagCreate, TagResponse

router = APIRouter(prefix="/api/admin/tags", tags=["admin-tags"])

@router.post("", response_model=TagResponse, status_code=201)
def create(data: TagCreate, db: Session = Depends(get_db), _: User = Depends(get_current_user)):
    if db.query(Tag).filter(Tag.slug == data.slug).first():
        raise HTTPException(status_code=400, detail="Slug already exists")
    tag = Tag(**data.model_dump())
    db.add(tag)
    db.commit()
    db.refresh(tag)
    return tag

@router.put("/{tag_id}", response_model=TagResponse)
def update(tag_id: int, data: TagCreate, db: Session = Depends(get_db), _: User = Depends(get_current_user)):
    tag = db.query(Tag).filter(Tag.id == tag_id).first()
    if not tag:
        raise HTTPException(status_code=404, detail="Tag not found")
    for field, value in data.model_dump().items():
        setattr(tag, field, value)
    db.commit()
    db.refresh(tag)
    return tag

@router.delete("/{tag_id}")
def delete(tag_id: int, db: Session = Depends(get_db), _: User = Depends(get_current_user)):
    tag = db.query(Tag).filter(Tag.id == tag_id).first()
    if not tag:
        raise HTTPException(status_code=404, detail="Tag not found")
    db.delete(tag)
    db.commit()
    return {"message": "Tag deleted"}
```

- [ ] **Step 3: 注册路由到 app/main.py**

```python
from app.routers.admin import categories as admin_categories, tags as admin_tags
# ...
app.include_router(admin_categories.router)
app.include_router(admin_tags.router)
```

- [ ] **Step 4: Commit**

```bash
git add app/routers/admin/categories.py app/routers/admin/tags.py app/main.py
git commit -m "feat: add admin CRUD for categories and tags"
```

---

## Task 11: 管理员评论 API

**Files:**
- Create: `backend/app/routers/admin/comments.py`
- Modify: `backend/app/main.py`

- [ ] **Step 1: 创建 app/routers/admin/comments.py**

```python
from fastapi import APIRouter, Depends, HTTPException
from sqlalchemy.orm import Session
from app.dependencies import get_db, get_current_user
from app.models.comment import Comment
from app.models.user import User
from app.schemas.comment import CommentAdminResponse

router = APIRouter(prefix="/api/admin/comments", tags=["admin-comments"])

@router.get("", response_model=list[CommentAdminResponse])
def list_comments(
    approved: bool | None = None,
    db: Session = Depends(get_db),
    _: User = Depends(get_current_user),
):
    q = db.query(Comment)
    if approved is not None:
        q = q.filter(Comment.is_approved == approved)
    return q.order_by(Comment.created_at.desc()).all()

@router.put("/{comment_id}/approve", response_model=CommentAdminResponse)
def approve(comment_id: int, db: Session = Depends(get_db), _: User = Depends(get_current_user)):
    comment = db.query(Comment).filter(Comment.id == comment_id).first()
    if not comment:
        raise HTTPException(status_code=404, detail="Comment not found")
    comment.is_approved = True
    db.commit()
    db.refresh(comment)
    return comment

@router.delete("/{comment_id}")
def delete(comment_id: int, db: Session = Depends(get_db), _: User = Depends(get_current_user)):
    comment = db.query(Comment).filter(Comment.id == comment_id).first()
    if not comment:
        raise HTTPException(status_code=404, detail="Comment not found")
    db.delete(comment)
    db.commit()
    return {"message": "Comment deleted"}
```

- [ ] **Step 2: 注册路由到 app/main.py**

```python
from app.routers.admin import comments as admin_comments
# ...
app.include_router(admin_comments.router)
```

- [ ] **Step 3: Commit**

```bash
git add app/routers/admin/comments.py app/main.py
git commit -m "feat: add admin comment moderation API"
```

---

## Task 12: 搜索 API

**Files:**
- Create: `backend/app/routers/public/search.py`
- Modify: `backend/app/main.py`

- [ ] **Step 1: 创建 app/routers/public/search.py**

```python
from fastapi import APIRouter, Depends, Query
from sqlalchemy.orm import Session
from app.dependencies import get_db
from app.models.post import Post
from app.schemas.post import PostResponse

router = APIRouter(prefix="/api/search", tags=["search"])

@router.get("", response_model=list[PostResponse])
def search(
    q: str = Query(..., min_length=1),
    db: Session = Depends(get_db),
):
    keyword = f"%{q}%"
    return (
        db.query(Post)
        .filter(
            Post.deleted_at.is_(None),
            Post.status == "published",
            (Post.title.ilike(keyword)) | (Post.content.ilike(keyword)),
        )
        .order_by(Post.created_at.desc())
        .limit(20)
        .all()
    )
```

- [ ] **Step 2: 注册路由到 app/main.py**

```python
from app.routers.public import search
# ...
app.include_router(search.router)
```

- [ ] **Step 3: 写测试**

新建 `tests/test_public_posts.py`：

```python
def test_list_posts_empty(client):
    res = client.get("/api/posts")
    assert res.status_code == 200
    assert res.json() == []

def test_search_returns_matching_posts(auth_client, client):
    auth_client.post("/api/admin/posts", json={
        "title": "Python Tips",
        "slug": "python-tips",
        "content": "Learn Python fast",
        "status": "published",
    })
    res = client.get("/api/search?q=Python")
    assert res.status_code == 200
    assert len(res.json()) == 1
    assert res.json()[0]["slug"] == "python-tips"

def test_search_excludes_drafts(auth_client, client):
    auth_client.post("/api/admin/posts", json={
        "title": "Draft Post",
        "slug": "draft-post",
        "content": "Draft content",
        "status": "draft",
    })
    res = client.get("/api/search?q=Draft")
    assert res.status_code == 200
    assert res.json() == []
```

- [ ] **Step 4: 运行所有测试**

```bash
pytest tests/ -v
```

预期：所有测试 PASSED，无 FAILED

- [ ] **Step 5: Commit**

```bash
git add app/routers/public/search.py app/main.py tests/test_public_posts.py
git commit -m "feat: add search API and public post tests"
```

---

## 完成验收

- [ ] 启动服务：`uvicorn app.main:app --reload`
- [ ] 打开 `http://localhost:8000/docs`，确认所有接口都出现在 Swagger UI 中
- [ ] 用 Swagger UI 手动测试登录流程：POST `/api/auth/login` → 用返回的 cookie 访问 `/api/admin/posts`
- [ ] 运行完整测试套件：`pytest tests/ -v --tb=short`
- [ ] 确认所有测试通过后，后端第一阶段完成，可以开始前端计划
